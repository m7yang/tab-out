# Engineering Change Ledger

Completed proposal bodies stay with their effort under `.scratch/` after their
durable decisions move into code, tests, `CONTEXT.md`, or an ADR. This ledger
keeps a concise index without replacing the full local tracker history.

| Wave | Outcome | Commits |
| --- | --- | --- |
| Animation audit (001–004) | Unified motion tokens and exit timing, narrowed transition properties, and aligned the source-switch indicator curve. | `ac28415`, `038fd4f`, `61562e3`, `9fa5606` |
| Architecture audit (005–008) | Added the Browser Tabs Gateway, shared FLIP move module, and suppression-tone view-model data. The proposed shared title-expansion measurer was rejected; [ADR-0002](adr/0002-title-expansion-measurement-stays-per-surface.md) records why the per-surface engines remain separate. | `3db7cb6`, `e62cbeb`, `73f71a0`, `cd460ef`, `31fc77c` |
| React audit (009–012) | Stabilized App seams, restored compiler coverage for chip and history hot paths, and surfaced source-switch failures. | `8350a33`, `b0ba829`, `60b8ee8`, `7361a3c` |
| Activation History reorder follow-up | Replaced pointer-only gating with visibility-first survivor FLIP behavior, including one deferred move after hidden commits while preserving immediate tab focus and existing removal motion. | `0a1f9dab`, `ae54a53c` |
| Effect 4 adoption continuation (038–071) | Established one shared runtime per extension entry, composed complete browser, storage, startup, command, and tooling workflows, scoped owned timers and reconnects, and enforced the saturated boundary. [ADR-0014](adr/0014-adopt-effect-behind-dashboard-intake-seams.md) records retained and rejected opportunities, including the lazy-source chunk-graph rejection. | 34 phase commits, `ca8afec5` through `df45f1eb` |
| Architecture deepening (2026-09) | Extracted the Page Chip text-layout engine behind a chip-owned session seam ([ADR-0026](adr/0026-chip-text-layout-engine-behind-a-chip-owned-seam.md)) with node-pure policy tests and a real-Chrome fixture spec, then lifted the Activation History row (measurement engine, expansion/actions hooks, row components) into `src/components/history-entry/` behind the panel-facing `HistoryEntry` and measurement-flush interface. Row shape preserved; ADR-0002's per-surface engines stand. The toast plumbing collapsed from six files onto its two real seams (extension presenter registry; lazy page runtime chunk). | `614dda8a`, `6cb0eafb`, `c70c139f`, `0d3300f1` |
| Domain-card view-model decomposition (2026-09) | Decomposed `computeDomainCardViewModel` from one 2,326-line closure web into pure modules under `src/extension/domain-card-view-model/`: the title-suppression engine (seed rows in, presentations out), display-segment builders, the same-title chip-draft compile pass, ordering and page-pin factories, tab liveness predicates, display dedupe and subdomain grouping, suppression scoping with singleton inlining, and chip builders behind one explicit build context. The 783-line facade keeps the per-compute caches and section assembly as the composition root. Behavior unchanged; locked by the existing view-model, render-pipeline, and highlight suites. | `2e4e946d`, `1d8b79c5`, `c159b623`, `76acdc0e`, `6adb99fa`, `3f55e3f7`, `6daa89db`, `98f5e76a` |
| Declarative migration completion (2026-09) | Rewrote the remaining loop-and-accumulate derivations as pipelines (history-panel row candidates with one bounded priority fold; filter-highlight ranges via RegExp.escape and matchAll; bionic fixation ranges), consolidated the nine cross-tooltip mutable singletons into `src/components/ui/tooltip-coordination.ts` with named transitions (event-handler state, deliberately not a store subscription), moved the startup-order debug effects behind `use-startup-order-debug.ts`, and extracted the FLIP hand-off protocol (primes, source-switch staging by request id, commit-side consume, parked intra-card pin move) into `src/extension/card-move-choreography.ts` behind a lazily created instance so mission refs are read only at event/commit time. Behavior, delays, and dependency arrays unchanged; verified by the full suites plus the dashboard-layout and smoke browser specs. | `1ccf7b02`, `bfc291f2`, `5632879a`, `7292d20b`, `c7c25e5d`, `b0b115d6` |
| Smoke spec page-realm migration (2026-09) | Moved the dashboard smoke spec's 142 inline CDP source-text evaluations into typed, closure-free page functions under `tests/browser/page-realm/`, run through `evaluateInPage` (Playwright `page.evaluate` under the navigation retry); the CDP session remains for trusted input and viewport emulation, and `waitForBrowserCondition` stays the only source-text boundary. Typing surfaced nullable geometry and raw focus-update arguments, now behind small typed helpers, and the spec shrank from 7,170 to 4,203 lines. The intermittent context-menu dismissal was traced to Floating UI clearing its trigger-press mark on a zero-delay timer and fixed by settling one macrotask after a menu opens. `docs/agents/verification.md` records the convention. | `66eaf781`, `73c43282`, `0b0ab9ff`, `04dcedaa`, `8b29dedc`, `a4ac97cf`, `c8bc798e`, `56c9abc1`, `42e8eb5c` |
| Imperative DOM markers (2026-09) | Moved three hand-written DOM markers onto React-owned state: the Domain Card close fade renders from a `cardClosing` state, the header `data-scrolled` flag renders from the `DashboardPinnedTop` wrapper that owns the sentinel IntersectionObserver, and pinned-domain drag feedback flows through the `domain-reorder-feedback` store so only the source and hovered target cards re-render. Two real-pointer drag specs, a `?pinnedDomains=` fixture seed, and a store node suite cover the change. The grouped-chip hover marker, the measurement and FLIP engines, pre-React boot markers, focus recovery, and the body drag-cursor flag stay imperative by design; filter-result keyboard selection remains a deferred candidate. | `47119869`, `8a76baa3`, `bc9c49c3` |
| Imperative plumbing (2026-09) | Replaced latest-value mirror refs with `useEffectEvent` in the refresh hook, tooltip wheel handler, and merge host, and moved the masonry hook's render-phase ref writes into a layout effect. Shared the window/document listener wiring (`useWindowEvent`, `useDocumentEvent`), the pointer-drag lifecycle (`startPointerDrag`, used by the pinned-domain reorder and the history scrollbar), and the outside-activity close that PageChip and Activation History rows had duplicated. The pathgroup label observer now follows its element through a callback ref with cleanup instead of a state-keyed effect. Kept by design: stable-callback refs in filter routing and the title-expansion controller, the history panel's child-shared filter ref, and the merge host's mount-once status listeners. | `dbcd7044`, `102d70ec`, `fb856497` |

The React Compiler baseline is enforced by
[`scripts/react-compiler-check.ts`](../scripts/react-compiler-check.ts).
Runtime behavior contracts remain in [`AGENTS.md`](../AGENTS.md) and
[`CONTEXT.md`](../CONTEXT.md).
